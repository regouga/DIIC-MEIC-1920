name: Companion_App
description: Companion for the Eco Footprint
publish_to: 'none'

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  settings_ui: ^0.1.1
  charts_flutter: ^0.6.0
  bezier_chart: "^1.0.6"

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_blue: 0.6.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/